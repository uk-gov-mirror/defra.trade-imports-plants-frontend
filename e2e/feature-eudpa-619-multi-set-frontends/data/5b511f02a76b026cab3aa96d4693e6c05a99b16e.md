# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: e2e/features/admin/frontend-seed-context.spec.ts >> Frontend seed context >> refuses a page the frontend rejected, rather than seeding half a notification
- Location: tests/e2e/features/admin/frontend-seed-context.spec.ts:49:3

# Error details

```
Error: expect(received).rejects.toThrow(expected)

Expected pattern: /responded 400/
Received message: "POST /live-animals/notifications/notifications/origin responded 500 instead of a redirect. Body:
<!DOCTYPE html>
<html lang=\"en\" class=\"govuk-template\">
  <head>
    <meta charset=\"utf-8\">
    <title>Something went wrong - Import notification service - GOV.UK</title>
    <meta name=\"viewport\" content=\"width=device-width, initial-scale=1, viewport-fit=cover\">
    <meta name=\"theme-color\" content=\"#1d70b8\">·
  <meta name=\"csrf-token\" content=\"\">
  <link href=\"/public/stylesheets/application.5ceb9aa.min.css\" rel=\"stylesheet\">·
      <link rel=\"icon\" sizes=\"48x48\" href=\"/public/assets/images/favicon.ico\">
      <link rel=\"icon\" sizes=\"any\" href=\"/public/assets/images/favicon.svg\" type=\"image/svg+xml\">
      <link rel=\"mask-icon\" href=\"/public/assets/images/govuk-icon-mask.svg\" color=\"#1d70b8\">
      <link rel=\"apple-touch-icon\" href=\"/public/assets/images/govuk-icon-180.png\">
      <link rel=\"manifest\" href=\"/public/assets/manifest.json\">·
  </head>
  <body class=\"govuk-template__body\">
    <script>document.body.className += ' js-enabled' + ('noModule' in HTMLScriptElement.prototype ? ' govuk-frontend-supported' : '');</script>·
        <a href=\"#main-content\" class=\"govuk-skip-link\" data-module=\"govuk-skip-link\">Skip to main content</a>········
        <header class=\"govuk-template__header\">···············
<div class=\"govuk-header app-header\">
  <div class=\"govuk-header__container govuk-width-container\">
    <div class=\"govuk-header__logo\">
      <a href=\"https://www.gov.uk/\" class=\"govuk-header__homepage-link\">
          <svg
            focusable=\"false\"
            role=\"img\"
            xmlns=\"http://www.w3.org/2000/svg\"
            viewBox=\"0 0 324 60\"
            height=\"30\"
            width=\"162\"
            fill=\"currentcolor\" class=\"govuk-header__logotype\" aria-label=\"GOV.UK\"
          ><title>GOV.UK</title>    <g>
                <circle cx=\"20\" cy=\"17.6\" r=\"3.7\"/>
                <circle cx=\"10.2\" cy=\"23.5\" r=\"3.7\"/>
                <circle cx=\"3.7\" cy=\"33.2\" r=\"3.7\"/>
                <circle cx=\"31.7\" cy=\"30.6\" r=\"3.7\"/>
                <circle cx=\"43"

      79 |     // A page that rejects its payload can re-render 200 with an error summary, so a non-redirect is a failed post.
      80 |     if (!isRedirect(response)) {
    > 81 |       throw new FrontendFormError(response.status(), path, (await response.text()).slice(0, BODY_EXCERPT_LENGTH));
         |             ^
      82 |     }
      83 |
      84 |     const location = response.headers().location;

      at FrontendFormClient.postForm (file:/app/adapters/http/frontend-form-client.ts:81:13)
      at file:/app/tests/e2e/features/admin/frontend-seed-context.spec.ts:53:5
      at node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1655:9
      at node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1135:11
      at TimeoutManager.withRunnable (node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:403:14)
      at TestInfoImpl._runWithTimeout (node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1133:7)
      at node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1653:7
      at WorkerMain._runTest (node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1626:5)
      at WorkerMain.runTestGroup (node_modules/@playwright/test/node_modules/playwright/lib/worker/workerProcessEntry.js:1521:9)
      at process.<anonymous> (node_modules/@playwright/test/node_modules/playwright/lib/common/index.js:1941:25)
```

# Test source

```ts
  1  | import { SET_BASES } from '@page-objects/base/sets';
  2  | 
  3  | import { test, expect } from '@fixtures';
  4  | import { MongoDbClient } from '@adapters/db/mongodb-client';
  5  | import { type NotificationDocument } from '@domain/models/db/notification-document';
  6  | import { timeouts } from '@config/timeouts';
  7  | import { skipUnlessComposeEnvironment } from '@utils/playwright/environment';
  8  | 
  9  | const referenceNumberFrom = (redirectPath: string) => redirectPath.split('/')[2];
  10 | 
  11 | // Auth state is minted per Playwright project, so this proof only holds from the admin
  12 | // project: an e2e-project session is already a frontend session.
  13 | test.describe('Frontend seed context', { tag: ['@integration', '@mongodb'] }, () => {
  14 |   test.beforeEach(() => {
  15 |     skipUnlessComposeEnvironment('the seed proof asserts on Mongo directly, which only the compose stack exposes');
  16 |   });
  17 | 
  18 |   test('seeds an origin answer into the notification document from an admin-project spec', async ({ frontendForms }) => {
  19 |     const created = await frontendForms.postForm(`${SET_BASES.liveAnimals}/notifications`);
  20 |     expect(created).toMatch(new RegExp(`^${SET_BASES.liveAnimals}/notifications/[^/]+/origin$`));
  21 | 
  22 |     const referenceNumber = referenceNumberFrom(created);
  23 |     await frontendForms.postForm(`${SET_BASES.liveAnimals}/notifications/${referenceNumber}/origin`, {
  24 |       countryOfOrigin: 'FR',
  25 |       regionOfOriginCodeRequirement: 'no',
  26 |       regionOfOriginCodeSuffix: '',
  27 |       internalReferenceNumber: 'Imports456GB',
  28 |     });
  29 | 
  30 |     const client = new MongoDbClient();
  31 |     try {
  32 |       await client.connect();
  33 |       const collection = client.collection<NotificationDocument>('trade-imports-animals-backend', 'notification');
  34 |       await expect.poll(() => collection.countDocuments({ referenceNumber }), { timeout: timeouts.short }).toBe(1);
  35 | 
  36 |       const [doc] = await collection.find({ referenceNumber }).toArray();
  37 |       expect(doc.notification.origin).toEqual({
  38 |         countryCode: 'FR',
  39 |         requiresRegionCode: 'no',
  40 |         regionOfOriginCode: '',
  41 |         internalReference: 'Imports456GB',
  42 |       });
  43 |       expect(doc.fulfilments?.length).toBeGreaterThan(0);
  44 |     } finally {
  45 |       await client.close();
  46 |     }
  47 |   });
  48 | 
  49 |   test('refuses a page the frontend rejected, rather than seeding half a notification', async ({ frontendForms }) => {
  50 |     const created = await frontendForms.postForm(`${SET_BASES.liveAnimals}/notifications`);
  51 |     const referenceNumber = referenceNumberFrom(created);
  52 | 
  53 |     await expect(
  54 |       frontendForms.postForm(`${SET_BASES.liveAnimals}/notifications/${referenceNumber}/origin`, { countryOfOrigin: 'Narnia' }),
> 55 |     ).rejects.toThrow(/responded 400/);
     |               ^ Error: expect(received).rejects.toThrow(expected)
  56 |   });
  57 | 
  58 |   test('seeds a complete journey into every section of the notification document', async ({ seededJourney, addressBookApi }) => {
  59 |     const consignor = await addressBookApi.findByName('Astra Rosales');
  60 |     const placeOfOrigin = await addressBookApi.findByName('Origin Farm');
  61 | 
  62 |     const referenceNumber = await seededJourney.createDraftNotification('readyToSubmit');
  63 | 
  64 |     const client = new MongoDbClient();
  65 |     try {
  66 |       await client.connect();
  67 |       const collection = client.collection<NotificationDocument>('trade-imports-animals-backend', 'notification');
  68 |       await expect.poll(() => collection.countDocuments({ referenceNumber }), { timeout: timeouts.short }).toBe(1);
  69 | 
  70 |       const [doc] = await collection.find({ referenceNumber }).toArray();
  71 |       const { notification } = doc;
  72 |       const [complement] = notification.commodity.commodityComplement;
  73 |       const [species] = complement.species;
  74 | 
  75 |       expect(doc.status).toBe('DRAFT');
  76 |       expect(notification.origin.countryCode).toBe('FR');
  77 |       expect(species.earTag).toBe('UK123456789012');
  78 |       expect(complement.totalNoOfAnimals).toBe(1);
  79 |       expect(notification.reasonForImport).toBe('internalMarket');
  80 |       expect(notification.additionalDetails.unweanedAnimals).toBe('no');
  81 |       expect(notification.consignor?.addressId).toBe(consignor.id);
  82 |       expect(notification.placeOfOrigin?.addressId).toBe(placeOfOrigin.id);
  83 |       expect(notification.placeOfOrigin?.address?.postcode).toBe(placeOfOrigin.postcode);
  84 |       expect(notification.cphNumber).toBe('123456789');
  85 |       expect(notification.transport.portOfEntry).toBe('GB ABD');
  86 |       expect(notification.transport.arrivalDate).toBeDefined();
  87 |       expect(notification.transport.transporter?.approvalNumber).toBe('ES-T2-45001294');
  88 |       expect(notification.consignment?.name).toBe('Animal and Plant Health Agency');
  89 |     } finally {
  90 |       await client.close();
  91 |     }
  92 |   });
  93 | });
  94 | 
```